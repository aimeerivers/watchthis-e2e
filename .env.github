# Environment variables for GitHub Actions CI
# These are test environment values, safe to commit to repository

# Service URLs - using localhost since services run in the same runner
HOME_SERVICE_URL=http://localhost:7279
MEDIA_SERVICE_URL=http://localhost:7769
SHARE_SERVICE_URL=http://localhost:8372
USER_SERVICE_URL=http://localhost:8583

# Test credentials - these are for testing only, not production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=AdminPassword1

# Node environment
NODE_ENV=test

# Test timeouts and configuration
TIMEOUT_SECONDS=30
HEADLESS=true
